---
:backends:
  - yaml

:hierarchy:
  - "%{::virtual}/%{::asfosname}"
  - "nodes/%{clientcert}"
  - "%{::asfosname}/%{::asfosrelease}"
  - "colo/%{asfcolo}"
  - "oem/%{::oem}"
  - "common"

:yaml:
  :datadir: '/tmp/kitchen/hiera'

