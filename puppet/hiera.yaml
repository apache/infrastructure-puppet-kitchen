---
:backends:
  - yaml

:hierarchy:
#  - "%{::virtual}/%{::asfosname}" # only enal be this if you use a caching service like apt-cacher-ng
#  - "nodes/%{::hostname}"
#  - "%{::asfosname}/%{::asfosrelease}"
#  - "colo/%{::asfcolo}"
#  - "oem/%{::oem}"
  - "nodes/wintest-windows"
  - "common"

:yaml:
  :datadir: '/etc/puppet/data'
  :datadir: 'C:\ProgramData\PuppetLabs\hiera\var'

