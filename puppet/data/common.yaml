classes_windows:
  - asf999_windows::create_user
  - datadog_agent

asf999_windows::create_user::user: 'asf999_windows'
asf999_windows::create_user::groups:
  - 'Users'
  - 'Administrators'

asf999_windows::create_user::password: 'putPassword_here' # do not commit plain text, still working on eyaml support.

datadog_agent::api_key: 'putkey_here' # do not commit plain text, still working on eyaml support.